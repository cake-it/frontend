import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  buttonView: {
    alignItems: 'center',
    gap: 20,
    marginBottom: 20,
  },
});
