import { View } from 'react-native';
import React from 'react';

// 해당 컴포넌트는 아직 UI 구현되지 않았음 -> 추후 구현 예정
const Indicator = () => {
  return (
    <View
      style={{
        width: '100%',
        height: 100,
        backgroundColor: '#EDEDED',
        marginTop: 20,
      }}
    />
  );
};

export default Indicator;

// const styles = StyleSheet.create({});
